#include <bits/stdc++.h>
#define size 100000
using namespace std;
typedef vector<string> vs;

int main(){
	int s,f;
	string line;
	cin>>s>>f;
	map<string , vs > m;
	while(s||f){
		getline(cin,line);
		while(line[0]-'0'>9){
			sscanf(c,"%s",st);
			if(m.find(st)!=m.end())
				m.insert(st,vs());
			while()
			fscanf(c,size,stdin);
		}
		// while(sscanf
		sscanf(c,"%d %d",&s,&f);
	}
}